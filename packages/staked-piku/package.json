{
  "name": "staked-piku",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "deploy:staked-piku": "forge script script/StakeDeployment.sol:StakeDeployment --broadcast --verify -vvvv",
    "clean": "forge clean",
    "with-env": "dotenv -e ./.env --"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "glob": "^10.3.3"
  }
}
