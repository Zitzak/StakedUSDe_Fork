{
  "name": "stakedpiku-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "deploy": "turbo run deploy",
    "generate-abis": "turbo run generate-abis"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "packageManager": "yarn@1.22.18"
}
